services:
  mongo:
    image: mongo:7
    container_name: opint-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - opint-mongo-data:/data/db

volumes:
  opint-mongo-data:
